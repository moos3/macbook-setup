---
- hosts: localhost
  user: moose
  connection: local

  vars:
    dock_tile_size: 45
    dock_large_tile_size: 50
    dock_auto_hide: true

  vars_files:
    - vars/main.yml

  roles:
    - geerlingguy.homebrew
    - dotfiles
    - macosx-setup

  vars_prompt:
    - name: "githubtoken"
      prompt: "Enter your github peronsal access token"
      private: no
